# Canonical production URL. Optional on Vercel, where
# VERCEL_PROJECT_PRODUCTION_URL is used automatically.
NEXT_PUBLIC_SITE_URL=

# VaultX download counter (Upstash Redis via Vercel Storage).
# Either naming scheme works.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Secret for GET /api/download-count?key=...
STATS_SECRET=
